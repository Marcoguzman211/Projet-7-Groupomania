const dotenv = require('dotenv')
dotenv.config()

module.exports = {
    mysql_database: {
        host: "localhost",
        user: 'groupomania',
        password: 'OcrAccess7',
        database: 'groupodb'
    }
}